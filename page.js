"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {createClient} from "@/lib/supabase";import DataTable from "@/components/DataTable";
export default function Staff(){const [rows,setRows]=useState([]);async function load(){const {data}=await createClient().from("staff").select("*").order("created_at",{ascending:false});setRows(data||[])}useEffect(()=>{load()},[]);async function del(id){if(confirm("Delete this staff member?")){await createClient().from("staff").delete().eq("id",id);load()}}return <><div className="flex justify-between"><h1 className="text-3xl font-bold">Staff</h1><Link href="/dashboard/staff/new" className="btn-primary">Add staff</Link></div><div className="mt-5"><DataTable columns={[{key:"employee_no",label:"Employee"},{key:"first_name",label:"First name"},{key:"last_name",label:"Last name"},{key:"role",label:"Role"}]} rows={rows} actions={r=><button onClick={()=>del(r.id)} className="text-red-400">Delete</button>}/></div></>}
