"use client";
import {useState} from "react";import {useRouter} from "next/navigation";import {createClient} from "@/lib/supabase";import TeacherForm from "@/components/teachers/TeacherForm";
export default function NewTeacher(){const [busy,setBusy]=useState(false),router=useRouter();async function save(form){setBusy(true);const {error}=await createClient().from("teachers").insert(form);setBusy(false);if(error)alert(error.message);else router.push("/dashboard/teachers")}return <><h1 className="mb-6 text-3xl font-bold">Add teacher</h1><div className="card p-6"><TeacherForm onSubmit={save} busy={busy}/></div></>}
