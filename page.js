"use client";
import {useState} from "react";import {useRouter} from "next/navigation";import {createClient} from "@/lib/supabase";import ClassForm from "@/components/classes/ClassForm";
export default function NewClass(){const [busy,setBusy]=useState(false),router=useRouter();async function save(form){setBusy(true);const {error}=await createClient().from("classes").insert(form);setBusy(false);if(error)alert(error.message);else router.push("/dashboard/classes")}return <><h1 className="mb-6 text-3xl font-bold">Add class</h1><div className="card p-6"><ClassForm onSubmit={save} busy={busy}/></div></>}
