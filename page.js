"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {createClient} from "@/lib/supabase";import DataTable from "@/components/DataTable";
export default function Classes(){const [rows,setRows]=useState([]);async function load(){const {data}=await createClient().from("classes").select("*").order("name");setRows(data||[])}useEffect(()=>{load()},[]);async function del(id){if(confirm("Delete class?")){await createClient().from("classes").delete().eq("id",id);load()}}return <><div className="flex justify-between"><h1 className="text-3xl font-bold">Classes</h1><Link href="/dashboard/classes/new" className="btn-primary">Add class</Link></div><div className="mt-5"><DataTable columns={[{key:"name",label:"Class"},{key:"section",label:"Section"},{key:"capacity",label:"Capacity"},{key:"room_number",label:"Room"}]} rows={rows} actions={r=><button onClick={()=>del(r.id)} className="text-red-400">Delete</button>}/></div></>}
